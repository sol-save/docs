## Push changes
When you want to push local commits to chain, go ahead and run the following command:

```bash
gitsol push
```

or

```bash
npx gitsol push
```

This will prepare and upload your files to IPFS, then send a transaction to the gitsol contract to add the file hash. All under the hood!
