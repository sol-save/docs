## Push changes to a repository

To push changes to a repo, run the gitsol push command.

```bash
gitsol push
```

You can also use `gitsol add` and `gitsol commit -m 'commit-message'` to add and commit changes to the repo.
